# Compile babelhub as a release build by default (nimble build, nim c, Docker).
# It's a server binary — there's no reason to ship the debug build.
-d:release
# Needed for the GitHub OAuth HTTPS calls (std/httpclient over TLS).
-d:ssl
